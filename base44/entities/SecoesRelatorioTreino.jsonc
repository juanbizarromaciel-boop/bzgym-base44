{
  "name": "SecoesRelatorioTreino",
  "type": "object",
  "properties": {
    "relatorioEvolucaoTreinoId": {
      "type": "string"
    },
    "usuarioId": {
      "type": "string"
    },
    "assinanteId": {
      "type": "string"
    },
    "titulo": {
      "type": "string"
    },
    "tipoSecao": {
      "type": "string",
      "enum": [
        "capa",
        "resumo",
        "grafico",
        "tabela",
        "analise",
        "comparativo",
        "alerta",
        "conclusao"
      ]
    },
    "conteudoHtml": {
      "type": "string"
    },
    "dadosJson": {
      "type": "object"
    },
    "ordem": {
      "type": "number"
    },
    "visivelNoPdf": {
      "type": "boolean",
      "default": true
    }
  },
  "required": [
    "relatorioEvolucaoTreinoId",
    "titulo",
    "tipoSecao"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "personal"
          }
        },
        {
          "user_condition": {
            "role": "assinante"
          }
        },
        {
          "data.usuarioId": "{{user.email}}"
        },
        {
          "data.assinanteId": "{{user.email}}"
        }
      ]
    },
    "read": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "personal"
          }
        },
        {
          "user_condition": {
            "role": "assinante"
          }
        },
        {
          "data.usuarioId": "{{user.email}}"
        },
        {
          "data.assinanteId": "{{user.email}}"
        }
      ]
    },
    "update": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "personal"
          }
        },
        {
          "data.usuarioId": "{{user.email}}"
        },
        {
          "data.assinanteId": "{{user.email}}"
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "data.usuarioId": "{{user.email}}"
        },
        {
          "data.assinanteId": "{{user.email}}"
        }
      ]
    }
  }
}